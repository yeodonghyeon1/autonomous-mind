List all active cron jobs using CronList.

Find the cron job with description containing "autonomous-mind" and delete it using CronDelete.

Confirm deletion and report the final cycle number from `vault/.state.json`.
